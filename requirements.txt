# Nutribot Backend — Dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0

# Redis
redis[hiredis]>=5.0.0
# HTTP client
httpx>=0.27.0

# OpenAI
openai>=1.60.0

# Audio conversion
pydub>=0.25.0
# ffmpeg must be installed on the system

# Scheduling (for sweepers)
# Using asyncio tasks instead of APScheduler for simplicity

pypdf>=4.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
